# 🧪 Тестирование системы бронирования

## ✅ Что было реализовано

### Шаг 1 и 2: Создание бронирования ✓

- [x] Передача `guideId` и `cityId` в форму бронирования
- [x] Проверка авторизации пользователя
- [x] Создание записи в таблице `bookings` через API
- [x] Email уведомление гиду о новом запросе
- [x] Редирект туриста на `/traveler/bookings` с сообщением об успехе
- [x] Отображение бронирований в дашборде гида `/guide/bookings`

---

## 📝 Как проверить, что всё работает

### Предварительные требования

1. **База данных настроена** - проверьте, что таблица `bookings` существует
2. **Есть минимум 2 аккаунта:**
   - Аккаунт туриста (traveler)
   - Аккаунт гида (guide) с `approved` статусом
3. **SMTP настроен** (опционально) - для тестирования email уведомлений

---

## 🎬 Пошаговое тестирование

### Тест 1: Создание бронирования как турист

1. **Откройте страницу гида**
   ```
   http://localhost:3000/guides/test-87a4c0fc
   ```
   (Замените `test-87a4c0fc` на реальный slug или ID вашего гида)

2. **Войдите как турист**
   - Если вы не авторизованы, нажмите "Request to Book" → вас перенаправит на `/auth/sign-in`
   - Войдите под аккаунтом с ролью `traveler`

3. **Заполните форму бронирования:**
   - Duration: выберите 4, 6 или 8 часов
   - Date: выберите будущую дату
   - Time: выберите время (например, 10:00)
   - Travelers: выберите количество человек (1-8)
   - Meeting location: выберите опцию
   - Special requests: добавьте заметку (опционально)

4. **Отправьте запрос:**
   - Нажмите "Request to Book"
   - Должен появиться индикатор загрузки "Creating request..."

5. **Проверьте редирект:**
   - Вы должны быть перенаправлены на `/traveler/bookings`
   - Должно появиться зелёное сообщение об успехе: "Booking request sent!"

6. **Проверьте бронирование в списке:**
   - На странице `/traveler/bookings` перейдите на вкладку "Pending"
   - Ваше новое бронирование должно отображаться
   - Статус должен быть "Pending"

---

### Тест 2: Просмотр бронирования как гид

1. **Выйдите из аккаунта туриста**
   ```
   http://localhost:3000/auth/sign-out
   ```

2. **Войдите как гид**
   - Войдите под аккаунтом с ролью `guide`
   - ID гида должен совпадать с тем, кому отправляли запрос

3. **Откройте страницу бронирований:**
   ```
   http://localhost:3000/guide/bookings
   ```

4. **Проверьте вкладку "Requests":**
   - Должно отображаться созданное бронирование
   - Рядом с "Requests" должен быть счётчик: `Requests (1)`
   - В карточке бронирования должны быть:
     - Имя туриста
     - Город
     - Дата и время
     - Длительность
     - Статус "Pending"
     - Цена

5. **Проверьте sidebar:**
   - В левом меню рядом с "Bookings" должен быть badge с числом pending запросов

---

### Тест 3: Проверка в базе данных (опционально)

Откройте Supabase SQL Editor и выполните:

```sql
-- Проверить созданное бронирование
SELECT 
  b.*,
  t.full_name as traveler_name,
  g.full_name as guide_name,
  c.name as city_name
FROM bookings b
LEFT JOIN profiles t ON b.traveler_id = t.id
LEFT JOIN profiles g ON b.guide_id = g.id
LEFT JOIN cities c ON b.city_id = c.id
WHERE b.status = 'pending'
ORDER BY b.created_at DESC
LIMIT 5;
```

**Что проверить:**
- [x] `traveler_id` - должен быть ID вашего аккаунта туриста
- [x] `guide_id` - должен быть ID гида
- [x] `city_id` - должен совпадать с городом гида
- [x] `status` - должен быть `'pending'`
- [x] `starts_at` - должна быть выбранная дата/время
- [x] `ends_at` - должна быть `starts_at + duration_hours`
- [x] `duration_hours` - должно быть 4, 6 или 8
- [x] `price_total` - должна быть правильная цена (база + service fee)
- [x] `currency` - должна быть валюта гида (USD/EUR)

---

### Тест 4: Email уведомления (если настроен SMTP)

1. **Проверьте .env файл:**
   ```bash
   RESEND_API_KEY=your_resend_api_key
   EMAIL_FROM=noreply@yourdomain.com
   ```

2. **После создания бронирования:**
   - Гид должен получить email на адрес, привязанный к его аккаунту
   - Тема: "New booking request on Rainbow Tour Guides"
   - В письме должна быть ссылка на бронирование

3. **Если email не приходит:**
   - Проверьте логи в консоли браузера (F12)
   - Проверьте логи сервера (terminal где запущен `npm run dev`)
   - Email отправка настроена как "fire-and-forget", поэтому ошибки не блокируют создание бронирования

---

## 🐛 Типичные проблемы и решения

### Проблема: "Unable to connect to database"
**Решение:** Проверьте переменные окружения:
```bash
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
```

### Проблема: "Unauthorized" или редирект на sign-in
**Решение:** Убедитесь, что вы авторизованы как турист

### Проблема: "Failed to create booking request"
**Решение:** 
1. Проверьте консоль браузера (F12) → вкладка Network → найдите запрос `/api/bookings/create`
2. Проверьте ответ сервера - там будет детальное описание ошибки
3. Проверьте RLS политики в Supabase для таблицы `bookings`

### Проблема: Бронирование не отображается у гида
**Решение:**
1. Проверьте, что `guide_id` в бронировании совпадает с ID гида
2. Проверьте RLS политику для чтения бронирований гида
3. Попробуйте обновить страницу (F5)

### Проблема: Счётчик pending не обновляется в sidebar
**Решение:** 
- Счётчик загружается при рендере sidebar (SSR)
- Обновите страницу (F5) чтобы увидеть новый счётчик

---

## ✅ Чеклист успешного теста

- [ ] Форма бронирования открывается на странице гида
- [ ] Неавторизованных пользователей редиректит на sign-in
- [ ] Форма валидирует обязательные поля
- [ ] При отправке показывается индикатор загрузки
- [ ] После создания редиректит на `/traveler/bookings`
- [ ] Показывается сообщение об успехе
- [ ] Бронирование отображается во вкладке "Pending" у туриста
- [ ] Бронирование отображается во вкладке "Requests" у гида
- [ ] Счётчик в sidebar гида показывает количество pending
- [ ] В базе данных создана корректная запись
- [ ] Email отправлен гиду (если настроен SMTP)

---

## 🎯 Следующие шаги

После успешного тестирования можно переходить к:
- **Шаг 3-4:** Реализация Accept/Decline функционала для гида
- **Шаг 5:** Детальная страница бронирования
- **Шаг 6:** Финальное тестирование всего flow

---

## 📞 Если что-то не работает

1. Проверьте консоль браузера (F12)
2. Проверьте терминал с `npm run dev`
3. Проверьте Supabase Dashboard → Table Editor → bookings
4. Проверьте Supabase Dashboard → Authentication → Users
5. Отправьте скриншот ошибки для помощи
