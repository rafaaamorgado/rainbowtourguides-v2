# 🚀 Quick Fix Guide - Schema Issues

## Проблема
Страницы `/guides` и `/guides/[slug]` не работают из-за отсутствующих колонок в базе данных.

## Решение (5 минут)

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://supabase.com/dashboard
2. Выберите ваш проект
3. Откройте **SQL Editor** в левом меню

### Шаг 2: Выполните SQL скрипт
1. Откройте файл `supabase/FIX-ALL-SCHEMA-ISSUES.sql`
2. Скопируйте **весь** содержимое файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl/Cmd + Enter)

### Шаг 3: Проверьте результат
После выполнения вы должны увидеть:
- ✅ Сообщение "Schema fixes applied successfully!"
- Статистику по городам и гидам
- Список первых 5 гидов с их slug

### Шаг 4: Проверьте сайт
1. Откройте http://localhost:3000/guides
2. Кликните на любого гида
3. Страница должна открыться без ошибок!

## Что делает скрипт?

1. **Добавляет отсутствующие колонки в `cities`:**
   - `country_name`
   - `country_code`

2. **Добавляет отсутствующие колонки в `guides`:**
   - `about`
   - `headline`
   - `tagline`
   - `status`
   - `slug`
   - И другие...

3. **Генерирует slug для всех гидов:**
   - Формат: `имя-гида-12345678`
   - Пример: `luca-romano-cc31c92a`

4. **Исправляет RLS политики:**
   - Разрешает публичный доступ к гидам
   - Гиды могут редактировать свой профиль

5. **(Опционально) Одобряет всех гидов:**
   - Закомментировано по умолчанию
   - Раскомментируйте строки 73-75 если нужно

## Альтернативный способ (через терминал)

Если у вас настроен Supabase CLI:

```bash
cd supabase
psql $DATABASE_URL -f FIX-ALL-SCHEMA-ISSUES.sql
```

## Проверка ошибок

Если после выполнения скрипта все еще есть ошибки:

1. **Проверьте логи терминала:**
   ```
   Ищите строки с "Error:" или "❌"
   ```

2. **Проверьте колонки в базе:**
   ```sql
   SELECT column_name 
   FROM information_schema.columns
   WHERE table_name = 'guides';
   ```

3. **Проверьте RLS политики:**
   ```sql
   SELECT * FROM pg_policies WHERE tablename = 'guides';
   ```

## Помощь

Если проблема не решена:
1. Скопируйте полный текст ошибки из консоли
2. Проверьте результаты verification queries в конце SQL скрипта
3. Убедитесь что все миграции применены

---

**Время выполнения:** ~30 секунд  
**Безопасность:** Скрипт использует `IF NOT EXISTS` - безопасно запускать несколько раз
